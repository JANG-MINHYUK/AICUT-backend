[phases.setup]
nixPkgs = [
  "python311Full",
  "mesa",
  "libGL",
  "pkg-config"
]

[phases.install]
cmds = [
  "python3 -m pip install --break-system-packages -r requirements.txt"
]

[phases.start]
cmd = "PATH=$PATH:/root/.local/bin gunicorn app:app --bind 0.0.0.0:$PORT"
