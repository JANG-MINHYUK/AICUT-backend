[phases.setup]
nixPkgs = [
  "python311Full",  # Python 3.11 포함
  "mesa",
  "libGL",
  "pkg-config",
  "pip"
]

[phases.install]
cmds = [
  "python3 -m pip install --break-system-packages -r requirements.txt"
]

[phases.start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
