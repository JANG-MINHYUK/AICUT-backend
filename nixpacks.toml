[phases.setup]
nixPkgs = [
  "python311Full",
  "mesa",
  "libGL",
  "pkg-config"
]

[phases.install]
cmds = [
  "/nix/store/*python3*/bin/pip3 install --break-system-packages -r requirements.txt"
]

[phases.start]
cmd = "/nix/store/*python3*/bin/gunicorn app:app --bind 0.0.0.0:$PORT"
